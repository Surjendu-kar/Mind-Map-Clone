import { Box } from "@mui/material";
import Header from "../Header/Header";

function ConfigureChatbot() {
  return (
    <Box>
      <Header
        heading={"Personalize the chatbot"}
        title={`Customize the look & feel of the chatbot: colors, personality, chatbot name, logo, business actions, and much more!`}
      />
    </Box>
  );
}

export default ConfigureChatbot;
